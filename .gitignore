# ── Secrets — NIKDY do gitu ─────────────────────────────────
secrets/
*.env
*.env.*
!*.env.example
!.env.example
compose/postgres/pgbouncer/userlist.txt

# ── Velké / runtime věci ────────────────────────────────────
iso/
compose/postgres/data/
compose/monitoring/data/
compose/supabase/supabase-docker/
compose/supabase/.tmp-supabase/

# ── launchmail (build artefakty a závislosti) ───────────────
launchmail/node_modules/
launchmail/**/node_modules/
launchmail/.turbo/
launchmail/**/.turbo/
launchmail/**/.next/
launchmail/**/dist/
launchmail/docker/

# lokální dump/cutover artefakty — NIKDY do gitu
backups/

# stack env soubory se secrety — nikdy do gitu
compose/**/.env

# Python validation artefacts
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
