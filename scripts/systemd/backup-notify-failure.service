[Unit]
Description=Telegram alert pri selhani homelab zalohy

[Service]
Type=oneshot
StandardInput=data
StandardInputText=❌ NIGHTLY BACKUP SELHAL (backup.service). Zkontroluj journalctl -u backup.service -n 50
ExecStart=/srv/homelab/self-healing/notify.sh
